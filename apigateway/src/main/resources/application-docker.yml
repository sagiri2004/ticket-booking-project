spring:
  data:
    redis:
      host: redis
      port: 6379
      password: null
      timeout: 2000ms

eureka:
  instance:
    hostname: gateway-service
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka
      register-with-eureka: true
      fetch-registry: true
